{
  "name": "@repo/infra",
  "version": "0.1.0",
  "description": "Infrastructure deployment for all apps",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy:docs": "pulumi up -s docs",
    "deploy:web": "pulumi up -s web",
    "preview:docs": "pulumi preview -s docs",
    "preview:web": "pulumi preview -s web",
    "destroy:docs": "pulumi destroy -s docs",
    "destroy:web": "pulumi destroy -s web"
  },
  "dependencies": {
    "@pulumi/aws": "^6.0.0",
    "@pulumi/pulumi": "^3.0.0",
    "@repo/pulumi-nextjs-aws": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22.13.9",
    "typescript": "^5.7.3"
  }
}